apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kmg-tls-cert
  namespace: kmg-ns
spec:
  secretName: kmg-tls-secret
  dnsNames:
    - localhost
  issuerRef:
    name: kmg-ca-issuer
    kind: ClusterIssuer
    group: cert-manager.io
